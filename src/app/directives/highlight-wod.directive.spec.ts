import { HighlightWodDirective } from './highlight-wod.directive';

describe('HighlightWodDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightWodDirective();
    expect(directive).toBeTruthy();
  });
});
