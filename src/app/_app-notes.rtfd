-- Install and run storybook
 npx -p @storybook/cli sb init --type angular (npm run storybook - to launch)

-- iterate over words in straighten
function abbreviate(string) {
    var words = string.split(" ");
    var answer = "";
    for (var i = 0; i < words.length; i += 1) {

        var count = words[i].length - 2;
        var last = words[i].charAt(words[i].length - 1);
        answer= answer + words[i][0] + count + last;
    }
    return answer;
}
